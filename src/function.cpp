#include "function.h"

void Function::VerifyFunction() {
  //Verify();
  bool reactants = false;
  bool products = false;
  /*for (const auto &reaction : reactions) {
    for (const auto &specie : reaction.reactants) {
      for (int i = inputSpecies.capacity())
    }
  } */
}

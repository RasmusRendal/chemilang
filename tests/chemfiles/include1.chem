import tests/chemfiles/include2.chem;

module MulAdditions {
	input: [a, b, c, d];
	private: [x, y];
	output: [z];
	compositions: {
		x = Addition(a, b);
		y = Addition(c, d);
	}
	reactions: {
		x + y -> x + y + z;
		z -> 0;
	}
}
